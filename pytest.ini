[pytest]
# Basic pytest configuration
addopts = -v --tb=short

# pytest-asyncio configuration (only used if pytest-asyncio is installed)
# This enables automatic detection of async test functions
asyncio_mode = auto

